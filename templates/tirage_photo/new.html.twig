{% extends 'base.html.twig' %}

{% block title %}New TiragePhoto{% endblock %}

{% block body %}
    <h1 class='text-center mb-3'>Choisissez vos photo</h1>

    {# {{ include('tirage_photo/_form.html.twig') }} #}


     <form method='POST'>
        <div class="mb-3">
        <p>

         <label for="photo">Photos</label>
            <select name="photo" id="photos">
                    <option value="">--choisissez la photo--</option>
                {% for photo in photos %}
            
                <option value="{{photo.id}}" id='photo'>{{photo.id}}</option>
                
                {% endfor %}
            </select>
            {# <label for="photo">Photos</label>
            {% for photo in photos %}
                <input type="checkbox" id="{{photo.id}}" name="photo">
                <label for="{{photo.id}}"><img src="{{ asset('uploads/tirage/' ~ photo.photo) }}" class="card-img-top" alt="tirage" style="width:50px"></label>
            {% endfor %} #}
        </p>       
        <p>
            <label for="tirage">tirage</label>
            <select name="tirage" id="tirage">
                    <option value="">--choisissez le tirage--</option>
                {% for tirage in tirages %}
            
                <option value="{{tirage.id}}" id='tirage'>{{tirage.tirage}}</option>
                
                {% endfor %}
            </select>
            
            <div id='prix'></div>
                    
            <script>
                let prix = document.getElementById('prix');
                let tirages = document.getElementById('tirage');
                console.log(tirages)
                tirages.addEventListener('click', function(){
                    tirages.addEventListener("change", () => {
                        prix.innerHTML = tirages.value;
                    })
                })
            </script>
        </p>
            <p>
                <label for="quantite">Quantité</label>
                <input type="number" id="quantite" name="quantite"
                    min="1" max="100">
            </p>
            <button class='btn btn-primary m-3'>Valider</button>
        </div>
    </form>
      
    {% if photos %}


     


        {# <table class="table">
            <thead>
                <tr>
                    <th>Photo</th>
                    <th>format</th>
                    <th>QuantiteTirage</th>
                    <th>Prix</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            
            {% for photo in photos %}
                <tr>
                    <td><img src="{{ asset('uploads/tirage/' ~ photo.photo) }}" class="card-img-top" alt="tirage" id='photo_tirage'></td>
                    
                        <td></td>
                        <td></td>
                        
       
                    <td> #}
                        {# {{ include('photo/_delete_form.html.twig') }} #}
                        {# <a href="{{ path('app_tirage_photo_edit', {'id': tirage_photo.id}) }}">edit</a> #}
                   {# </td>
                   <td>
                 prix:
             </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
            {% endfor %} 
            </tbody>
             </table>
             <hr>
             {% for tiragep in tirage_photos %}
                 {{tiragep.tirage.prix|number_format(-1)}}
             {% endfor %}
              #}
    {% endif %}
{% endblock %}
