<form method="post" action="{{ path('app_deco_murale_delete', {'id': deco_murale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ deco_murale.id) }}">
    <button class="btn">Delete</button>
</form>
